mutation updatePlace($id: String!, $place: PlaceInput!) {
  updatePlace(id: $id, place: $place) {
    id
    favorite
    googlePlaceId
    latitude
    longitude
    name
    checkedInToday
    createdAt
  }
}
