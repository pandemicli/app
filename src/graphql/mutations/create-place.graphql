mutation createPlace($place: PlaceInput!) {
  createPlace(place: $place) {
    id
    favorite
    googlePlaceId
    location {
      latitude
      longitude
    }
    name
    checkedInToday
    createdAt
  }
}
