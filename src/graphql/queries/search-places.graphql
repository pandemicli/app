query searchPlaces(
  $query: String!
  $language: String!
  $latitude: Float!
  $longitude: Float!
) {
  searchPlaces(
    query: $query
    language: $language
    latitude: $latitude
    longitude: $longitude
  ) {
    id
    name
    latitude
    longitude
  }
}
