query todayFeed($date: String!) {
  todayFeed(date: $date) {
    contacts {
      id
      interactedToday
      name
    }
    places {
      id
      checkedInToday
      name
    }
    symptoms {
      experiencedToday
      name
    }
  }
}
